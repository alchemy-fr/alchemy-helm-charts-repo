{{- with .Values.keycloak }}
{{- if .enabled }}
apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: phrasea
    tier: keycloak
{{- end }}
{{- end }}
